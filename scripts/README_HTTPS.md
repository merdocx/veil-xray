# Скрипты для настройки HTTPS

Этот каталог содержит скрипты для настройки и проверки HTTPS для Veil Xray API.

## 📋 Доступные скрипты

### 1. `check_https_ready.sh` - Проверка готовности к настройке HTTPS

Проверяет систему перед настройкой HTTPS.

**Использование:**
```bash
sudo bash scripts/check_https_ready.sh
```

**Проверяет:**
- Настройки домена в конфигурации
- DNS записи и соответствие IP
- Открытость портов 80 и 443
- Настройки firewall (UFW)
- Доступность API сервера на порту 8000
- Установку Nginx и Certbot

**Рекомендуется запускать перед `setup_https.sh`**

---

### 2. `setup_https.sh` - Автоматическая настройка HTTPS

Основной скрипт для автоматической настройки HTTPS.

**Использование:**
```bash
sudo bash scripts/setup_https.sh
```

**Выполняет:**
- Установку Nginx (если не установлен)
- Установку Certbot (если не установлен)
- Создание конфигурации Nginx из шаблона
- Получение SSL сертификата от Let's Encrypt
- Настройку автоматического обновления сертификатов
- Настройку редиректа HTTP → HTTPS

**Требования:**
- Права root
- Домен должен указывать на IP сервера
- Порты 80 и 443 должны быть открыты

---

### 3. `verify_https.sh` - Проверка корректности настройки HTTPS

Проверяет корректность настройки HTTPS после установки.

**Использование:**
```bash
sudo bash scripts/verify_https.sh
```

**Проверяет:**
- Корректность конфигурации Nginx
- Наличие и валидность SSL сертификата
- Срок действия сертификата
- Доступность API через HTTPS
- Корректность редиректа HTTP → HTTPS
- Настройку автообновления сертификатов

**Рекомендуется запускать после `setup_https.sh`**

---

## 📁 Файлы конфигурации

### `nginx-veil-xray-api.conf` - Шаблон конфигурации Nginx

Шаблон конфигурации Nginx для reverse proxy с HTTPS.

**Содержит:**
- Настройки HTTP сервера с редиректом на HTTPS
- Настройки HTTPS сервера
- Современные параметры SSL (TLS 1.2/1.3)
- Заголовки безопасности (HSTS, X-Frame-Options и др.)
- Проксирование на FastAPI приложение (порт 8000)

**Использование:**
Автоматически используется скриптом `setup_https.sh`. При ручной настройке скопируйте в `/etc/nginx/sites-available/veil-xray-api` и замените `veil-bear.ru` на ваш домен.

---

## 🔄 Типичный процесс настройки

```bash
# 1. Проверка готовности
sudo bash scripts/check_https_ready.sh

# 2. Настройка HTTPS
sudo bash scripts/setup_https.sh

# 3. Проверка после установки
sudo bash scripts/verify_https.sh
```

---

## 🛠 Устранение проблем

### Проблема: Скрипт не может получить сертификат

**Решение:**
1. Проверьте DNS: `dig your-domain.com`
2. Убедитесь, что порты 80 и 443 открыты
3. Проверьте, что API сервер запущен: `systemctl status veil-xray-api`

### Проблема: Nginx не запускается

**Решение:**
1. Проверьте конфигурацию: `nginx -t`
2. Просмотрите логи: `journalctl -u nginx -n 50`

### Проблема: HTTPS недоступен

**Решение:**
1. Проверьте статус Nginx: `systemctl status nginx`
2. Проверьте сертификат: `certbot certificates`
3. Просмотрите логи: `tail -f /var/log/nginx/veil-xray-api-error.log`

---

## 📚 Дополнительная документация

- Подробная инструкция: `/root/HTTPS_SETUP.md`
- Общая документация: `/root/README.md`



