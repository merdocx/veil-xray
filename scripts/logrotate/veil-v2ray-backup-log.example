# Install: sudo cp veil-v2ray-backup-log.example /etc/logrotate.d/veil-v2ray-backup-log
# Adjust path if project is not in /root/veil-v2ray

/root/veil-v2ray/logs/backup.log {
    monthly
    rotate 12
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
