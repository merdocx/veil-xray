# SLO: защита от перегрузки (шаблон)

> **Заполненный эталон для prod:** [SERVER_PROFILE.md](../SERVER_PROFILE.md).

Заполните и храните рядом с репозиторием или в вики. Пороги после **базовой линии** (см. `02-monitoring-baseline.md`) скорректируйте по графикам.

## Цель доступности

Опишите в одном абзаце, что нельзя допустить (пример):

- SSH и сервисы управления остаются отзывчивыми при любой нагрузке VPN.
- Допустима деградация VPN при внешней атаке, но не «падение» всего хоста.

**Владелец порогов (имя / роль):** ___________________

**Дата последнего пересмотра:** ___________________

---

## Таблица метрик и порогов

| Метрика | Как смотреть | Предупреждение | Критично | Действие |
|--------|----------------|----------------|----------|----------|
| load average 1m / N_cpu | Netdata / `uptime` | например > 0.85 | например > 1.5 | наблюдать / ужесточить policy / отказ новым |
| MemAvailable (МБ) | `/proc/meminfo` / Netdata | например < 512 | например < 200 | … |
| TCP established на порту 443 | `ss -tan sport = :443` | заполнить по базовой линии | … | … |
| RSS процесса xray | Netdata / `ps` | … | … | … |
| (опционально) smoke latency | `ping` / curl до тестового хоста | … | … | … |

**Длительность для «тревоги»** (чтобы не реагировать на секундные всплески): например **> 5 минут** подряд.

**Гистерезис** (если включён отказ новым): порог **включения** режима стресса ≠ порог **выключения**.
